import { FilterMedicinePipe } from './filter-medicine.pipe';

describe('FilterMedicinePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMedicinePipe();
    expect(pipe).toBeTruthy();
  });
});
